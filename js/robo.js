function annoyed() {
    var gif = document.getElementById("robo");
    if (gif.style.display === "none") {
        gif.style.display = "block"; } 
    else {
        gif.style.display = "none"; }
}
/*This function tests if element is displayed and hides it if it is and returns it to view if it is hidden*/